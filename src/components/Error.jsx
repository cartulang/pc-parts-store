const errorStyles = {
  transform: "translate(-50%,-50%)",
  position: "absolute",
  top: "50%",
  left: "50%",
};

const Error = () => {
  return <h1 style={errorStyles}>Something went wrong</h1>;
};

export default Error;
